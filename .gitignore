# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# venv (uv-managed)
.venv/

# editors / OS
.vscode/
.idea/
.DS_Store

# local-only Claude config (project-level overrides, not for repo)
.claude/settings.local.json

# benchmark outputs: per-prompt JSONL is for local debug only.
# Only the per-run summary (results/<run>.json) is committed.
results/*.jsonl

# personal notes — not for the repo
TODO.md
PLAN.md
